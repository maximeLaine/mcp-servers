{
  "name": "mcp-servers",
  "private": true,
  "workspaces": [
    "servers/*",
    "agents"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces"
  }
}
